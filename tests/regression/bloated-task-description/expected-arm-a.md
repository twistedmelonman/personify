Target shape, not a string to diff against:

> **Title:** Partner feed ingesting into staging, ready to evaluate
>
> **Description:**
>
> We can now pull the partner feed end to end and see what's in it, which we
> couldn't before. That's enough to decide whether the feed is worth building
> on before we spend anything on a real database. It's trial-scoped on
> purpose: flat-file index, single container, no managed DB. Monitoring is
> wired up. I don't know yet what the next phase needs.

The title states an end state a non-expert can evaluate. The body leads with
the capability, keeps the implementation choices in one clause, and admits the
open question rather than padding around it.
